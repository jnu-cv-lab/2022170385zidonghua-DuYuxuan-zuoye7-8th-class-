{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build C++ with OpenCV",
            "command": "g++ ${file} -g -o ${fileDirname}/build/main $(pkg-config --cflags --libs opencv4)",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "自动编译 C++ 和 OpenCV 代码"
        }
    ]
}